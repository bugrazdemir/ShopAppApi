using Domain.Models;
using Application.Common.Interfaces;
using MediatR;
using System.Numerics;
/*using System.Xml.Linq;
namespace Application.AddPart.Commands;

public class AddOrderCommand : IRequest<OrderAggregate>
{
    public AddOrderCommand(string orderNumber, double totalAmount, double discountAmount, DateTime orderDate)
    {
        OrderNumber = orderNumber;
        TotalAmount = totalAmount;
        DiscountAmount = discountAmount;
        OrderDate = orderDate;
    }

    public string OrderNumber { get; set; }
    public double TotalAmount { get; set; }
    public double DiscountAmount { get; set; }
    public DateTime OrderDate { get; set; }
    public virtual List<UserAggregate> Users { get; set; }
    public class Handler : IRequestHandler<AddOrderCommand, OrderAggregate>
    {
        private IMediator _mediator;
        private readonly IShopAppDbContext _dbContext;

        public Handler(IShopAppDbContext dbContext, IMediator mediator)
        {
            _dbContext = dbContext;
            _mediator = mediator;
        }

       */
}

